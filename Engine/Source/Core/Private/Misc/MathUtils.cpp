//
// Created by Egor Orachyov on 2019-10-08.
//

#include <Misc/MathUtils.h>
#include <math.h>

namespace Berserk {

    const float32 MathUtils::BIG_NUMBERf = 3.4e+38f;

    const float32 MathUtils::SMALL_NUMBERf = 1.e-8f;

    const float32 MathUtils::THRESH_FLOAT32f = 0.0001f;

    const float32 MathUtils::THRESH_POINT_ON_PLANEf = 0.10f;

    const float32 MathUtils::THRESH_POINTS_ARE_SAMEf = 0.00002f;

    const float32 MathUtils::THRESH_ZERO_NORM_SQUAREDf = 0.0001f;

    const float64 MathUtils::PI = M_PI;

    const float64 MathUtils::HALF_PI = M_PI_2;

    const float64 MathUtils::QUARTER_PI = M_PI_4;

    const float64 MathUtils::SQRT2 = M_SQRT2;

    const float64 MathUtils::E = M_E;

    const float32 MathUtils::PIf = (float32) M_PI;

    const float32 MathUtils::HALF_PIf = (float32) M_PI_2;

    const float32 MathUtils::QUARTER_PIf = (float32) M_PI_4;

    const float32 MathUtils::SQRT2f = (float32) M_SQRT2;

    const float32 MathUtils::Ef = (float32) M_E;

}